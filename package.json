{
  "name": "portfolio-demo-sites",
  "private": true,
  "description": "Portfolio hub plus five static demo websites. The sites need no build step; these dev tools only regenerate preview thumbnails.",
  "scripts": {
    "thumbs": "node tools/thumbs.js",
    "art": "python3 tools/gen_scenes.py",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "playwright": "^1.49.1"
  }
}
